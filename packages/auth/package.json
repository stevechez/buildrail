{
	"name": "@buildrail/auth",
	"version": "0.1.0",
	"private": true,
	"description": "Shared identity + organization-context helpers and role-based permission checks for all BuildRail apps.",
	"license": "UNLICENSED",
	"type": "module",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"default": "./src/index.ts"
		}
	},
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"files": [
		"src"
	],
	"scripts": {
		"typecheck": "tsc --noEmit",
		"build": "tsc --noEmit",
		"lint": "echo \"no lint configured for @buildrail/auth\""
	},
	"dependencies": {
		"@buildrail/types": "workspace:*",
		"@supabase/supabase-js": "^2",
		"server-only": "^0.0.1"
	},
	"devDependencies": {
		"typescript": "^5"
	}
}
